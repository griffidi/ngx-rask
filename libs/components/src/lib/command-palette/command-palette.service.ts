import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class CommandPaletteService {
  // #overlay = inject(Overlay);
  // open() {
  //   const portal = new ComponentPortal(CommandPalette);
  //   this.#overlay.create();
  // }
}
